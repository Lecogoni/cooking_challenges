<p id="notice"><%= notice %></p>

<h1>Les challenges</h1>

<table>
  <thead>
    <tr>
      
      <th>Title : </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @challenges.each do |challenge| %>
      <tr>
        <td><%= challenge.title %></td>
        <td><%= link_to 'Afficher', challenge %></td>
        <td><%= link_to 'Modifier', edit_challenge_path(challenge) %></td>
        <td><%= link_to 'Détruire', challenge, method: :delete, data: { confirm: 'Es-tu vraiment certain ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouveau challenge', new_challenge_path %>
