<div class="mb-5">
  <h1>Challenges en attentes de confirmation</h1>
</div>
      
<div class="col-lg-12 mb-5">
  <div class="bs-component">
    <ul class="list-group">
      <% @pending_challenges.each do |challenge| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= challenge.challenge.title.upcase %>
        invité par <%= challenge.user.username.capitalize %>
        <span class="badge badge-primary badge-pill"><%= challenge.participation %></span>
      </li>
      <% end %>
    </ul>
  </div>
</div>
